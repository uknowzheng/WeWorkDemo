接入过程中遇到的问题：
/banana/im/doLogin 接口登陆后获取的字段太少，缺少性别，邮箱地址，用户id（数据库id），微信ID（唯一性微信号，可能是个字符串，看看有没有必要，还是说跟英文用户名整合，主要用于登陆），昵称（用户名和昵称看看要不要合一起就叫用户名，主要用于展示），地区，签名等相关字段，如果不需要那么会把对应字段展示删了。

答复：
我们暂时只有这几个字段，这个用户不是常规的微信。需要的可以加上。但是都是空的没有数据

/banana/im/friends/getContacts
这个获取联系人的用户实体信息尽量跟上述要求的结构体保持一致。
答复：
用户和联系人的维度不是一样的，用户不是常规的微信，是多个微信的载体。联系人才是

/banana/im/chat/sendMessage
确认下是不是msgType主要都为：
消息类型 1000：系统消息 2001：文字消息 2002：图片消息 2003：语音消息 2004：视频消息 2005：图文链接 2006：名片消息 2007：表情消息 2010：文件消息 2013：小程序消息 2015：公众号消息
目前看了下文件和图片上传，需要后端提供上传文件的api，上传完成后，再把上传文件url的内容作为content，然后type设置为2003或2013，才能带到后端去，这里缺少一个文件上传接口，目前只能上传文字
答复：
暂时只做 1000 2001 2002 2004
上传接口我再提供，先预留

/banana/im/chat/dialogBox
返回结果要有个联系人头像，跟avatar区分开来，后续这个avatar可能表示群聊头像。缺少了聊天室的id（类似于chatId）。新增个lastMsgTime用于展示最新消息时间。
答复：
contactId 就是 chatId，这里没有做很强区分。
另外看看dialogBox返回的msgTime是否可以作为lastMsgTime。

/banana/im/chat/messageHistory
是不是加载完成dialogBox所有的聊天窗口信息之后，下一步要取最近的一定数量的信息，然后展示？
答复：
可以预先加载，也可以点进去聊天窗口再加载。

/banana/im/friends/getContacts
需要在获取的时候更新下在线的状态。用于更新每个人正常上下线
答复：
接口我改下

/banana/websocket/msg/{token}
收到新消息通知，需要吧对应消息dialogBox的ID给下发过来，不然不知道加到哪个窗口上
答复：
可以认为这个ID就是contactId，全文都是根据contactId作为联系人和窗口唯一主键